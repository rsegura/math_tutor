"""Canonical namespace for the primary mathematics tutoring bounded context.

All product code lives below :mod:`math_tutor`; ``src`` is only the configured
source root and is deliberately not a Python package.
"""
